import pantallas.Carga;

/**
 * La clase {@code Main} contiene el método principal de la aplicación.
 * En este ejemplo, el método {@code main} instancia un objeto de la clase {@code Carga}.
 * <p>
 * Este es el punto de entrada de la aplicación. Al ejecutarlo, se crea una instancia de
 * la clase {@code Carga}, donde se lanzará la pantalla de carga y se ejecutará todo.
 * </p>
 */
public class Main {
    /**
     * El método principal que se ejecuta cuando se inicia la aplicación.
     * Este método crea una nueva instancia de la clase {@code Carga}.
     *
     * @param args los argumentos de la línea de comandos (no se utilizan en este caso).
     */
    public static void main(String[] args) {
        new Carga();
    }
}
